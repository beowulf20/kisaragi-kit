// Package openrouter provides typed, client-wide OpenRouter provider routing
// over KKit's OpenAI-compatible chat client. Use ProviderPreferences.Only for
// a strict allowlist; Order controls priority and does not restrict membership
// by itself. Advanced routing fields remain available through ExtraFields.
package openrouter
